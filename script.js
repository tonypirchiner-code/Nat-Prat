function searchZip() {
        const zipCode = document.getElementById('zipCode').value;
        alert('Searching for properties in Zip Code: ' + zipCode);
        // You could add more functionality here, like redirecting to a search results page.
    }